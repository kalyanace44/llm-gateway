apiVersion: v1
kind: Secret
metadata:
  name: {{ include "llm-gateway.fullname" . }}-secrets
  labels:
    {{- include "llm-gateway.labels" . | nindent 4 }}
type: Opaque
data:
  admin-key: {{ .Values.gateway.adminKey | b64enc | quote }}
