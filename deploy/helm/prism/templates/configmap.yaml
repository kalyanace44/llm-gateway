apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "prism.fullname" . }}-config
  labels:
    {{- include "prism.labels" . | nindent 4 }}
data:
  prism.yaml: |
    {{- .Values.config | toYaml | nindent 4 }}
